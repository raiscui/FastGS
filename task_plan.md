# 任务计划: 主线索引续档

## [2026-03-28 08:47:34 UTC] [Session ID: 5957] [记录类型]: 主线 `task_plan.md` 续档并登记 `my6` 支线

### 目标
- 让新的主线 `task_plan.md` 回到“索引页”角色.
- 把 `my6` 的训练执行记录独立到 `__my6_35000` 支线上下文, 避免再次把主线写爆.

### 当前活跃支线索引
- `task_plan__my5_v6_filter_view7_highrot.md`
  - 主题: `my5` 的 `view 7` 高旋转筛帧对照.
  - 状态: 仍在推进, 不归档.
- `task_plan__my6_35000.md`
  - 主题: 按 `my5` 已验证口径处理 `/root/autodl-fs/my6`, 完成 `35000` 训练并产出视频与 `ply`.
  - 状态: 本轮新建, 后续执行细节统一写入对应后缀文件.

### 六文件摘要
- 默认主线最近的可复用结论:
  - `my5` 已验证的稳定口径已同步到 `EXPERIENCE.md`.
  - 后续遇到同类 `12` 视角生成视频目录, 可优先复用这条训练链.

### 当前状态
- 主线当前只保留索引与总览.
- `my6` 的计划、研究、执行与收尾统一转入 `__my6_35000` 支线上下文.

## [2026-03-28 11:48:07 UTC] [Session ID: codex-20260328-1148] [记录类型]: 登记 `my7` / `my8` 的双数据集 `35000` 支线

### 当前活跃支线索引
- `task_plan__my5_v6_filter_view7_highrot.md`
  - 主题: `my5` 的 `view 7` 高旋转筛帧对照.
  - 状态: 仍在推进, 不归档.
- `task_plan__my6_35000.md`
  - 主题: 按 `my5` 已验证口径处理 `/root/autodl-fs/my6`, 完成 `35000` 训练并产出视频与 `ply`.
  - 状态: 已完成交付, 当前保留作追溯记录.
- `task_plan__my7_my8_35000.md`
  - 主题: 复用 `my5/my6` 已验证口径, 处理 `/root/autodl-fs/my7` 与 `/root/autodl-fs/my8`, 完成各自的 `35000` 训练并产出视频与 `ply`.
  - 状态: 本轮新建, 后续执行细节统一写入对应后缀文件.

### 六文件摘要
- 已验证可复用规律:
  - 这类 `12` 视角目录默认走 `nomask`, 不把 `rendering_4D_maps/merged_mask.mp4` 接成训练 mask.
  - 单视角 `81` 帧、`16 fps` 的视频, 用 `5.333333333333 fps` 抽帧时会落到约 `27` 帧, 总图量约 `324`.
  - 长跑若遇到随机 CUDA 非法访问, 优先切 guarded 分段训练, 不要盲目整条重启.

### 当前状态
- 主线继续保持索引角色.
- `my7` / `my8` 的计划、研究、执行与收尾统一转入 `__my7_my8_35000` 支线上下文.

## [2026-03-28 12:05:00 UTC] [Session ID: 019d3478-98b2-7fa1-a395-f4ccc3012bf0] [记录类型]: 分析 FastGS 离线渲染链路与 Unity 画质差异

### 目标
- 搞清楚 FastGS 当前仓库的渲染输出使用了哪些组件与处理.
- 明确区分“仓库已验证事实”和“针对 Unity 插件画质差异的候选假设”.
- 给用户一份可操作的排查口径, 说明为什么 Unity 里常见会比离线 render 差.

### 阶段
- [x] 阶段1: 回读主线索引与经验
- [ ] 阶段2: 阅读渲染相关代码与文档
- [ ] 阶段3: 整理 Unity 画质差异的候选原因
- [ ] 阶段4: 形成结论并交付

### 关键问题
1. `render.py` 与 `gaussian_renderer` 实际调用了什么渲染实现?
2. 结果图是否叠加了额外的后处理, 还是主要差别来自训练/渲染配置本身?
3. Unity 插件常见的画质损失点, 哪些能从当前仓库代码得到静态证据支撑?

### 当前状态
- 已完成上下文回读.
- 正在阅读 `README.md`、`render.py`、`gaussian_renderer/`、`scene/` 与测试/脚本文档中的渲染线索.

## [2026-03-28 12:22:00 UTC] [Session ID: 019d3478-98b2-7fa1-a395-f4ccc3012bf0] [记录类型]: 完成渲染链路核对并形成交付结论

### 阶段状态
- [x] 阶段1: 回读主线索引与经验
- [x] 阶段2: 阅读渲染相关代码与文档
- [x] 阶段3: 整理 Unity 画质差异的候选原因
- [x] 阶段4: 形成结论并交付

### 已验证结论
- FastGS 当前仓库的离线渲染链路是 `render.py -> gaussian_renderer.render_fastgs -> diff_gaussian_rasterization_fastgs` CUDA 栅格器.
- 离线输出图没有看到额外的神秘后处理, 主要差异更可能来自表示能力、渲染口径、输入资产、分辨率与观察视角.
- 当前最强的候选解释是: Unity 插件没有完全复现官方 3DGS/FastGS 的完整表示或渲染口径, 或者导入资产不是最终 `iteration_xxx/point_cloud.ply`.

### 当前状态
- 已完成本轮代码与文档核对.
- 准备向用户交付“现象 -> 候选原因 -> 优先排查顺序”的中文结论.

## [2026-03-28 16:44:25 UTC] [Session ID: 019d339f-290f-7032-8726-3095f62295ac] [记录类型]: 登记 `my9` 的 `35000` 支线

### 当前活跃支线索引
- `task_plan__my5_v6_filter_view7_highrot.md`
  - 主题: `my5` 的 `view 7` 高旋转筛帧对照.
  - 状态: 仍在推进, 不归档.
- `task_plan__my6_35000.md`
  - 主题: 按 `my5` 已验证口径处理 `/root/autodl-fs/my6`, 完成 `35000` 训练并产出视频与 `ply`.
  - 状态: 已完成交付, 当前保留作追溯记录.
- `task_plan__my7_my8_35000.md`
  - 主题: 复用 `my5/my6` 已验证口径, 处理 `/root/autodl-fs/my7` 与 `/root/autodl-fs/my8`, 完成各自的 `35000` 训练并产出视频与 `ply`.
  - 状态: 已完成交付, 当前保留作追溯记录.
- `task_plan__my9_35000.md`
  - 主题: 按 `my5` 到 `my8` 已验证口径处理 `/root/autodl-fs/my9`, 完成 `35000` 训练并产出视频与 `ply`.
  - 状态: 本轮新建, 当前先排查输入路径是否真实存在.

### 六文件摘要
- 当前新发现的阻塞点:
  - 当前机器上的 `/root/autodl-fs` 与 `/autodl-fs/data` 都只看到 `my5`、`my6`、`my7`、`my8`.
  - 还没有发现 `my9` 目录.

### 当前状态
- 主线继续只保留索引角色.
- `my9` 的计划、核查与后续执行统一转入 `__my9_35000` 支线上下文.

## [2026-03-29 00:00:00 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: 登记 `my8` 漂浮棉絮伪影排查支线

### 当前活跃支线索引
- `task_plan__my5_v6_filter_view7_highrot.md`
  - 主题: `my5` 的 `view 7` 高旋转筛帧对照.
  - 状态: 仍在推进, 不归档.
- `task_plan__my6_35000.md`
  - 主题: 按 `my5` 已验证口径处理 `/root/autodl-fs/my6`, 完成 `35000` 训练并产出视频与 `ply`.
  - 状态: 已完成交付, 当前保留作追溯记录.
- `task_plan__my7_my8_35000.md`
  - 主题: 复用 `my5/my6` 已验证口径, 处理 `/root/autodl-fs/my7` 与 `/root/autodl-fs/my8`, 完成各自的 `35000` 训练并产出视频与 `ply`.
  - 状态: 已完成交付, 当前保留作追溯记录.
- `task_plan__my9_35000.md`
  - 主题: 按 `my5` 到 `my8` 已验证口径处理 `/root/autodl-fs/my9`, 完成 `35000` 训练并产出视频与 `ply`.
  - 状态: 已登记, 当前等待真实输入路径.
- `task_plan__my8_floaters.md`
  - 主题: 排查 `my8` 训练结果中“空中棉絮状 3DGS 漂浮”伪影, 区分是数据、位姿还是训练口径问题, 并给出处理建议.
  - 状态: 本轮新建, 后续分析与结论统一写入对应后缀文件.

### 六文件摘要
- 当前新增问题:
  - 用户反馈 `my8` 空中出现棉絮状漂浮 gaussians.
  - `my8` 与 `my7` 使用过同一套已验证流程, 因此适合先做同流程对照排查.

### 当前状态
- 主线继续只保留索引角色.
- `my8` 漂浮伪影的分析、证据、结论统一转入 `__my8_floaters` 支线上下文.

## [2026-03-29 11:27:44 UTC] [Session ID: codex-20260329T112615Z-af86adb4] [记录类型]: 登记 `my9` / `my10` 的联合 `35000` 支线

### 当前活跃支线索引
- `task_plan__my5_v6_filter_view7_highrot.md`
  - 主题: `my5` 的 `view 7` 高旋转筛帧对照.
  - 状态: 仍在推进, 不归档.
- `task_plan__my8_floaters.md`
  - 主题: 排查 `my8` 训练结果中的漂浮伪影.
  - 状态: 已登记, 当前保留追溯.
- `task_plan__my9_35000.md`
  - 主题: 先前单独处理 `my9` 的 `35000` 支线.
  - 状态: 历史支线, 已记录到“`input=324` 但 `prepare` 未闭环”的中间态.
- `task_plan__my9_my10_35000.md`
  - 主题: 按 `my5/my6/my7` 已验证口径, 联合处理 `/root/autodl-fs/my9` 与 `/root/autodl-fs/my10`, 完成各自的 `35000` 训练与视频/ply 导出.
  - 状态: 本轮新建, 后续计划、验证、执行与收尾统一写入对应后缀文件.

### 六文件摘要
- 已验证可直接复用的稳定口径:
  - `12` 视角生成视频目录
  - 训练走 `nomask`
  - `--video-fps 5.333333333333`
  - `-r 1`
  - `--densification_interval 500`
  - `--opacity_reset_interval 3000`
  - `--densify_until_iter 15000`
  - `--position_lr_max_steps 35000`
  - `--loss_thresh 0.1`
  - `--grad_thresh 0.0002`
  - `--grad_abs_thresh 0.0012`
  - `--highfeature_lr 0.005`
  - `--lowfeature_lr 0.0025`
  - `--dense 0.001`
  - `--mult 0.5`
  - `--optimizer_type default`
  - `--eval`
- 当前新增事实:
  - `my10` 已出现, 结构与 `my5` 到 `my9` 同类, 单视角抽帧同样是 `27` 帧.
  - `my9` 当前只有 `data/my9_colmap_fastgs/input=324` 与 `distorted/database.db`, 还没有 `images/` 和 `sparse/0`, 说明旧接力未完成闭环.

### 当前状态
- 主线继续只保留索引角色.
- `my9` / `my10` 本轮统一转入 `__my9_my10_35000` 支线上下文.
