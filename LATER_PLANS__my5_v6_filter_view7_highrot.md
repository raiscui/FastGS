## [2026-03-27 21:29:13 UTC] [Session ID: unknown-env-missing] 主题: `view 7` 筛图后续方向

### 待以后再做的方向
- 如果还想继续筛 `view 7`, 下一轮优先试:
  - 只动后段高 rotation 帧, 例如 `frame 10 / 11`
  - 避免再次制造 `2 -> 6` 这种超大断口
- 如果目标是服务后续 3DGS, 可以考虑:
  - 保留原图做 COLMAP
  - 只在训练采样阶段降低这些高风险帧权重
  - 这样能把 pose 求解稳定性和训练鲁棒性拆开验证

### 为什么先记在这里
- 当前 `v6` 已经证明:
  - 硬删除 `frame 3/4/5` 不是一个足够稳的主线策略
- 但“高 rotation 帧要不要弱化影响”这个方向仍然值得保留
