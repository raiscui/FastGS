# 任务计划: 排查 `my8` 空中棉絮状 3DGS 漂浮

## [2026-03-29 09:21:05 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: 新支线建档

### 目标
- 结合 `my8` 现有训练产物, 判断“空中棉絮状漂浮 gaussians”更像哪一类问题:
  - 输入素材中的悬浮颗粒 / 动态噪声
  - COLMAP 位姿或稀疏重建外点
  - FastGS 训练与 densify / prune 口径
- 给出按优先级排序的处理建议.
- 如仓库里已经有现成工具或既有经验, 要直接指出可复用入口.

### 两种方向
- 方案A(更稳, 最佳):
  - 用 `my7` 作为同流程正常对照组.
  - 对比 `my7` / `my8` 的训练产物、指标、点云统计与可用工具.
  - 先收敛问题类别, 再给精确处理建议.
- 方案B(更快, 先能用):
  - 直接给通用 3DGS 去漂浮经验清单.
  - 不利用当前仓库与 `my7` / `my8` 的现成证据.

### 当前决定
- 本轮先采用方案A.
- 原因:
  - 当前仓库里已经同时有 `my7` 与 `my8` 的完整产物.
  - 同流程对照能显著减少“把通用经验误当成已验证结论”的风险.

### 阶段
- [ ] 阶段1: 回读经验与相关支线
- [ ] 阶段2: 对比 `my7` / `my8` 训练产物与可疑差异
- [ ] 阶段3: 阅读仓库中可直接用于抑制漂浮噪点的代码与脚本
- [ ] 阶段4: 形成“现象 -> 假设 -> 验证建议 -> 处理方案”的交付

### 关键问题
1. `my8` 的漂浮更像训练出来的多余 gaussians, 还是输入里本来就有会被 3DGS 学进去的颗粒/亮点?
2. 当前仓库是否已经存在针对“空中颗粒 / 小亮点”这类噪声的 mask 或预处理工具?
3. 如果 `my7` 同参数正常、`my8` 异常, 最应该先改的是数据/掩码, 还是继续硬调训练超参数?

### 现象 -> 假设 -> 验证计划
- 现象:
  - 用户反馈 `my8` 空中有棉絮状 3DGS 漂浮.
  - `my8` 与 `my7` 使用过同一套 `nomask + 35000` 已验证流程.
- 当前主假设:
  - 问题优先像 `my8` 素材或场景本身包含会被 3DGS 学成半透明漂浮物的局部噪声, 而不是纯训练脚本 bug.
- 最强备选解释:
  - 也可能是 `my8` 的 COLMAP 位姿或稀疏点外点更差, 导致空中出现错误支撑点并在 densify 后被放大.
- 推翻主假设的证据:
  - 如果仓库证据表明 `my8` 的输入侧并无明显颗粒噪声, 但位姿/点云统计显著劣于 `my7`.
  - 或者存在已知训练参数口径会稳定制造这类漂浮伪影.

### 当前状态
- 目前在阶段1.
- 下一步先对比 `my7` / `my8` 的结果与仓库内现成工具, 再下结论.

## [2026-03-29 09:21:05 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: 完成证据回读与结论收敛, 进入交付

### 进度更新
- [x] 阶段1: 回读经验与相关支线
- [x] 阶段2: 对比 `my7` / `my8` 训练产物与可疑差异
- [x] 阶段3: 阅读仓库中可直接用于抑制漂浮噪点的代码与脚本
- [x] 阶段4: 形成“现象 -> 假设 -> 验证建议 -> 处理方案”的交付

### 已验证结论
- `my7` 与 `my8` 的训练入口参数一致:
  - `resolution=1`
  - `mask_dir=''`
  - `eval=True`
- 仓库已经把“空中漂浮亮点 / 灰尘”视为已知问题类型:
  - `README.md` 给出 `generate_particle_masks.py + --mask-dir` 的正式处理方式
  - `scripts/generate_particle_masks.py` 直接面向 `室内空中颗粒 / 尘埃 / 小亮点`
- `my8` 的空间点数量明显更高:
  - `input.ply`: `20789` vs `my7` 的 `12167`
  - `iteration_35000/point_cloud.ply`: `52683` vs `my7` 的 `35704`
- 抽查的 6 个对应帧没有支持“`my8` 输入里一定有更多亮颗粒”这条假设.
  - 因此这条解释只能保留为候选方向.

### 当前状态
- 已完成本轮分析.
- 下一步向用户交付优先级明确的处理顺序, 并给出可直接执行的命令建议.

## [2026-03-29 09:29:40 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: 按用户同意继续, 开始生成 `my8` 全量颗粒 mask

### 本轮动作
- 直接对 `data/my8_colmap_fastgs/images` 运行全量 `particle mask` 生成.
- 同时产出:
  - `masks_particle_v1`
  - `mask_debug_particle_v1`
  - `mask_summary_particle_v1.json`
- 生成完成后先检查:
  - 平均 mask 比例
  - 最重样本
  - 调试预览是否误伤大结构

### 当前状态
- 当前已从“分析阶段”进入“验证并准备落地阶段”.
- 下一步是拿到真实 mask 产物, 判断是否值得直接接上重训.

## [2026-03-29 09:29:40 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: mask 生成完成, 准备做带 mask 的短训练冒烟验证

### 已观察到的事实
- `mask_summary_particle_v1.json` 已成功生成.
- 关键统计:
  - `image_count = 324`
  - `average_mask_ratio = 0.0002142375846622085`
  - `max_mask_ratio = 0.0007443576388888889`
- 调试预览显示这版 mask 主要命中少量亮点 / 细小高亮区域, 当前没有看到大面积误伤.

### 本轮动作
- 先起一轮很短的带 mask 冒烟训练.
- 目标不是拿最终质量, 而是验证:
  - `--mask-dir` 链路可用
  - 训练能正常启动
  - 没有尺寸不匹配或数据读入错误

### 当前状态
- 当前进入“落地前的最小动态验证”.
- 冒烟通过后, 再决定是否直接起完整 `35000` 训练.

## [2026-03-29 09:29:40 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: 带 mask 冒烟训练通过, 启动完整 `35000` 重训

### 已验证结论
- `output/my8_mask_particle_smoke` 已成功完成 `100` 轮冒烟训练.
- 动态证据表明:
  - 训练明确打印 `Using mask directory: .../masks_particle_v1`
  - `324` 张相机读入成功
  - `100` 轮训练正常结束并保存点云
- 因此可以把“mask 链路本身有问题”这条担忧排除掉.

### 本轮动作
- 直接启动完整 `35000` 带 mask 重训, 输出目录:
  - `output/my8_mask_particle_v1`
- 训练完成后, 视时间继续做 `render` 导出, 便于和 `my8_nomask_v1` 对照.

### 当前状态
- 当前已从“最小动态验证”进入“正式落地执行”.

## [2026-03-29 09:29:40 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: 完整带 mask 训练在约 `21320` 轮触发 CUDA 运行时错误, 转入稳态续训排查

### 问题现象
- `my8_mask_particle_v1` 从头训练时, 在约 `21320 / 35000` 左右退出.
- 真实报错为:
  - `torch.AcceleratorError: CUDA error: operation not supported on global/shared address space`
  - 栈落在 `gaussian_renderer/__init__.py` 的 `visibility_filter : (radii > 0).nonzero()`

### 当前判断口径
- 现象已经确认.
- 但根因还不能直接写成“mask 导致”或“代码 bug 已确认”.
- 当前更稳的候选解释是:
  - 训练期某次 CUDA 栅格化 / 张量状态进入了不稳定态
  - 这类现象与此前项目里记录过的“长跑随机 CUDA 异常”同类

### 本轮动作
- 先核对仓库里是否已有现成的 guarded / segmented 续训入口.
- 如果有, 优先改走 `1000` 步一段的稳态训练链.
- 如果没有, 再手工拼接分段训练命令.

### 当前状态
- 当前从“正式训练”切到“故障收敛与稳态续训方案”.

## [2026-03-29 09:29:40 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: guarded 带 mask 训练已完成到 `35000`, 转入渲染导出

### 已验证结论
- `1000` 步一段的 guarded 训练已真实推进到 `35000`.
- 动态证据包括:
  - `12000->13000` 在 `seed=0` 失败后, `seed=1` 成功跨过
  - `33000->34000` 在 `seed=0` 失败后, `seed=1` 成功跨过
  - 最终日志明确输出: `[guard-my8-mask] completed through 35000`
- 因此当前可以确认:
  - 完整直跑不稳
  - 但 guarded 分段训练对 `my8_mask_particle_v1` 是有效交付路径

### 本轮动作
- 立即执行 `render` 导出 `35000` 轮 train/test 视频.
- 然后核对:
  - `ckpt_35000.pth`
  - `point_cloud/iteration_35000/point_cloud.ply`
  - `videos/train_iter35000.mp4`
  - `videos/test_iter35000.mp4`

### 当前状态
- 当前进入最终交付验证阶段.

## [2026-03-29 09:29:40 UTC] [Session ID: 019d38e4-25b6-7442-96f0-f2e4c43ccb82] [记录类型]: 带 mask 版本交付完成, 但效果结论为“链路成立, 提升未证实”

### 进度更新
- [x] 阶段1: 回读经验与相关支线
- [x] 阶段2: 对比 `my7` / `my8` 训练产物与可疑差异
- [x] 阶段3: 阅读仓库中可直接用于抑制漂浮噪点的代码与脚本
- [x] 阶段4: 形成“现象 -> 假设 -> 验证建议 -> 处理方案”的交付

### 最终状态
- 已完成:
  - `my8` particle mask v1 生成
  - 带 mask 冒烟验证
  - guarded `35000` 训练
  - `35000` train/test 视频导出
  - 与无 mask 版本的粗统计和标准指标对照
- 当前最终判断:
  - 这条路线已经被动态验证为“可执行”
  - 但目前还不能表述成“效果已明确优于无 mask 版本”
- 更值得继续推进的下一步:
  - 更有针对性的 particle mask 调参
  - 或直接切换到 COLMAP 外点 / 位姿连续性分析

## [2026-03-29 09:57:05 UTC] [Session ID: c1ad2430-46ff-4852-89ec-356e70f96f49] [记录类型]: 接手用户删图后的 `my8` 重跑请求, 切换到 pruned nomask 重建与训练

### 新现象
- 用户已手动修改 `data/my8_colmap_fastgs/input`:
  - `006_3` 全部删除
  - `005` 删除了一部分图片
- 当前动态核对结果:
  - `input = 277`
  - `images = 324`
  - `sparse/0 = 6`
- 因此如果直接训练旧数据根, 实际读到的仍然是上一次 `324` 张的旧 `images + sparse/0`, 不是用户刚刚删图后的输入.

### 两种方向
- 方案A(更稳, 本轮采用):
  - 新建一套 pruned 数据根 `data/my8_colmap_fastgs_input_pruned_v1`
  - 复制当前 `input` 到新根目录
  - 重新跑 COLMAP 生成新的 `images + sparse/0`
  - 再把 nomask 训练输出写到 `output/my8_nomask_input_pruned_v1`
- 方案B(更快, 但风险更高):
  - 直接覆盖旧的 `data/my8_colmap_fastgs/images` 和 `sparse/0`
  - 再复用旧输出目录重跑训练

### 当前决定
- 采用方案A.
- 原因:
  - 可以保留旧 `my8_nomask_v1` / `my8_mask_particle_v1` 作为可对照产物.
  - 避免把“删图后的新结果”和“历史结果”混在同一目录里, 后面更难核账.

### 本轮阶段
- [ ] 阶段1: 基于当前 `input=277` 建立新的 pruned 数据根
- [ ] 阶段2: 用图片模式重跑 COLMAP, 生成新的 `images + sparse/0`
- [ ] 阶段3: 用 nomask guarded `1000` 步分段训练到 `35000`
- [ ] 阶段4: 导出 `35000` 的 render 视频并核对最终产物
- [ ] 阶段5: 记录对照结果并完成交付

### 当前主假设
- 这次删掉整组 `006` 和部分 `005` 后, 新 COLMAP 的注册结果与后续漂浮情况会发生可见变化.
- 但在新的 `images + sparse/0` 真正生成前, 还不能把“漂浮一定会改善”写成已验证结论.

### 最强备选解释
- 即便删图后重建成功, 漂浮也可能仍主要来自 `my8` 剩余视角里的位姿外点或场景噪声.
- 那样的话, 本轮最多只能得到一版新的 nomask 对照, 不能自动推出“问题已解决”.

### 当前状态
- 当前进入阶段1.
- 下一步先创建 pruned 数据根并复制当前 `input`, 然后直接跑图片模式 COLMAP.

## [2026-03-29 10:00:03 UTC] [Session ID: c1ad2430-46ff-4852-89ec-356e70f96f49] [记录类型]: pruned 数据根已建立, 转入图片模式 COLMAP 重建

### 进度更新
- [x] 阶段1: 基于当前 `input=277` 建立新的 pruned 数据根
- [ ] 阶段2: 用图片模式重跑 COLMAP, 生成新的 `images + sparse/0`
- [ ] 阶段3: 用 nomask guarded `1000` 步分段训练到 `35000`
- [ ] 阶段4: 导出 `35000` 的 render 视频并核对最终产物
- [ ] 阶段5: 记录对照结果并完成交付

### 已验证结论
- `data/my8_colmap_fastgs_input_pruned_v1/input` 已复制完成.
- 动态复验结果:
  - `input = 277`
  - 目录结构正确, 没有出现嵌套错层.

### 当前状态
- 当前进入阶段2.
- 下一步直接对这套 pruned `input` 跑 `convert.py`, 生成新的 `images + sparse/0`.

## [2026-03-29 11:03:20 UTC] [Session ID: c1ad2430-46ff-4852-89ec-356e70f96f49] [记录类型]: pruned COLMAP 完成, 转入 nomask guarded `35000` 训练

### 进度更新
- [x] 阶段1: 基于当前 `input=277` 建立新的 pruned 数据根
- [x] 阶段2: 用图片模式重跑 COLMAP, 生成新的 `images + sparse/0`
- [ ] 阶段3: 用 nomask guarded `1000` 步分段训练到 `35000`
- [ ] 阶段4: 导出 `35000` 的 render 视频并核对最终产物
- [ ] 阶段5: 记录对照结果并完成交付

### 已验证结论
- pruned 数据根已完整可训练:
  - `input = 277`
  - `images = 277`
  - `registered_images = 277`
  - `points = 8515`
- 当前动态证据支持“删图后的 COLMAP 已经真正更新”, 不是复用了旧 `324` 张结果.

### 当前状态
- 当前进入阶段3.
- 下一步启动 `output/my8_nomask_input_pruned_v1` 的 guarded nomask 训练, 目标 `35000`.

## [2026-03-29 11:11:55 UTC] [Session ID: c1ad2430-46ff-4852-89ec-356e70f96f49] [记录类型]: 删图后的 pruned nomask 版本已完整交付, 本轮收尾

### 进度更新
- [x] 阶段1: 基于当前 `input=277` 建立新的 pruned 数据根
- [x] 阶段2: 用图片模式重跑 COLMAP, 生成新的 `images + sparse/0`
- [x] 阶段3: 用 nomask guarded `1000` 步分段训练到 `35000`
- [x] 阶段4: 导出 `35000` 的 render 视频并核对最终产物
- [x] 阶段5: 记录对照结果并完成交付

### 已验证结论
- 新版产物已齐:
  - `ckpt_35000.pth`
  - `iteration_35000/point_cloud.ply`
  - `train_iter35000.mp4`
  - `test_iter35000.mp4`
- 新版几何规模显著缩小:
  - `sparse points: 8515`
  - `final gaussians: 27220`
- 但标准指标明显差于旧版 `my8_nomask_v1`:
  - `SSIM: 0.8324 vs 0.8846`
  - `PSNR: 20.6860 vs 25.6491`
  - `LPIPS: 0.3403 vs 0.2308`

### 当前状态
- 本轮任务已完成.
- 下一步对外汇报时, 必须明确区分:
  - “这版已经跑完”
  - 和 “这版效果更好” 不是一回事.
