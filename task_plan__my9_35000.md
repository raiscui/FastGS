# 任务计划: 处理 `/root/autodl-fs/my9` 的 `35000` 训练与视频/ply 导出

## [2026-03-28 16:44:25 UTC] [Session ID: 019d339f-290f-7032-8726-3095f62295ac] [记录类型]: 新支线建档

### 目标
- 按 `my5` 到 `my8` 已验证的稳定口径处理 `/root/autodl-fs/my9`.
- 最终产出:
  - `COLMAP -> FastGS prepare`
  - `35000` 训练
  - `train/test` 视频
  - `3DGS ply`

### 两种方向
- 方案A(更稳, 最佳):
  - 先确认 `my9` 的真实路径与输入结构.
  - 再做单视角抽帧验证.
  - 确认可复用 `nomask + 5.333333333333 fps + guarded 35000` 后再启动全链路.
- 方案B(更快, 先能用):
  - 如果用户立刻给出正确路径, 直接按 `my5` 到 `my8` 的同口径开跑.
  - 中间只做最低限度结构检查.

### 当前决定
- 当前先走方案A.
- 原因:
  - 这次已经出现“路径不存在”的真实阻塞.
  - 在路径没确认前直接开跑只会制造空转和误判.

### 阶段
- [ ] 阶段1: 确认 `my9` 的真实路径与输入结构
- [ ] 阶段2: 做 `my9` 的最小动态抽帧验证
- [ ] 阶段3: 完成 `my9` 的 `prepare`
- [ ] 阶段4: 完成 `my9` 的 `35000` 训练与视频/ply 导出
- [ ] 阶段5: 回写支线 `notes/WORKLOG`, 收尾交付

### 关键问题
1. 用户给出的 `/root/autodl-fs/my9` 是否是最新路径, 还是数据还没同步到这台机器?
2. 如果路径写法不同, 真正的数据是否仍然属于和 `my5` 到 `my8` 一样的 `12` 视角生成视频目录?
3. 当前机器上是否已经存在可直接复用的 `my9` 中间产物目录?

### 现象 -> 假设 -> 验证计划
- 现象:
  - `/root/autodl-fs/my9` 不存在.
  - `/root/autodl-fs` 与 `/autodl-fs/data` 当前都只看到:
    - `my5`
    - `my6`
    - `my7`
    - `my8`
  - 在 `/root` 与 `/autodl-fs` 下按 `my9` / `*my9*` 搜索, 当前也没有命中目录.
- 当前主假设:
  - 更像是 `my9` 还没有同步到当前机器, 或者实际路径和用户说的不同.
- 最强备选解释:
  - 也可能 `my9` 被放在了当前搜索范围之外的其他挂载点.
- 推翻主假设的证据:
  - 如果后续在其他挂载点找到了完整的 `my9` 目录.
  - 或者用户提供了新的准确路径, 且该路径真实存在.

### 当前状态
- 当前卡在阶段1.
- 下一步不是启动训练, 而是先确认 `my9` 的真实路径.

## [2026-03-28 16:44:25 UTC] [Session ID: 019d339f-290f-7032-8726-3095f62295ac] [记录类型]: 用户再次确认同一路径后, 已做符号链接级复核

### 进度更新
- [ ] 阶段1: 确认 `my9` 的真实路径与输入结构
- [ ] 阶段2: 做 `my9` 的最小动态抽帧验证
- [ ] 阶段3: 完成 `my9` 的 `prepare`
- [ ] 阶段4: 完成 `my9` 的 `35000` 训练与视频/ply 导出
- [ ] 阶段5: 回写支线 `notes/WORKLOG`, 收尾交付

### 已验证结论
- `/root/autodl-fs` 不是独立真实目录, 而是符号链接:
  - `/root/autodl-fs -> /autodl-fs/data`
- 在真实目标目录 `/autodl-fs/data` 中, 当前只有:
  - `denoise_opt`
  - `my5`
  - `my6`
  - `my7`
  - `my8`
- `/autodl-fs/data/my9` 当前同样不存在.

### 当前状态
- 当前阻塞点仍是阶段1.
- 在真实数据目录不存在前, 不能进入抽帧和训练阶段.

## [2026-03-28 17:14:04 UTC] [Session ID: 019d339f-290f-7032-8726-3095f62295ac] [记录类型]: `my9` 路径已出现, 转入结构核查与最小动态验证

### 进度更新
- [ ] 阶段1: 确认 `my9` 的真实路径与输入结构
- [ ] 阶段2: 做 `my9` 的最小动态抽帧验证
- [ ] 阶段3: 完成 `my9` 的 `prepare`
- [ ] 阶段4: 完成 `my9` 的 `35000` 训练与视频/ply 导出
- [ ] 阶段5: 回写支线 `notes/WORKLOG`, 收尾交付

### 已验证结论
- 当前已重新确认:
  - `/root/autodl-fs/my9` 存在
  - `/autodl-fs/data/my9` 存在

### 当前状态
- 当前不再卡在“路径不存在”.
- 下一步立即核对目录结构和单视角抽帧结果.

## [2026-03-28 17:14:04 UTC] [Session ID: 019d339f-290f-7032-8726-3095f62295ac] [记录类型]: `my9` 结构与抽帧验证通过, 转入真实执行

### 进度更新
- [x] 阶段1: 确认 `my9` 的真实路径与输入结构
- [x] 阶段2: 做 `my9` 的最小动态抽帧验证
- [ ] 阶段3: 完成 `my9` 的 `prepare`
- [ ] 阶段4: 完成 `my9` 的 `35000` 训练与视频/ply 导出
- [ ] 阶段5: 回写支线 `notes/WORKLOG`, 收尾交付

### 已验证结论
- `my9` 当前目录结构与 `my5` 到 `my8` 一致:
  - 存在 `0..11` 共 `12` 个视角目录
  - 每个视角目录包含 `generated_videos/generated_video_0.mp4`
  - 同时存在 `rendering_4D_maps/merged_mask.mp4`
  - 顶层额外有 `shared/`, 但不影响沿用既有 RGB 发现口径
- 单视角视频元信息已确认:
  - `1280x720`
  - `16 fps`
  - `81` 帧
  - `duration=5.063s`
- 最小动态抽帧验证:
  - `5.333333333333 fps -> 27` 帧
- 当前没有既有中间产物:
  - `data/my9_colmap_fastgs = 0`
  - `output/my9_nomask_v1 = 0`

### 当前状态
- 当前进入阶段3.
- 下一步直接启动 `my9 prepare`, 并挂起 guarded 自动接力链.

## [2026-03-28 17:15:23 UTC] [Session ID: 019d339f-290f-7032-8726-3095f62295ac] [记录类型]: `my9 prepare` 与自动接力已启动

### 进度更新
- [x] 阶段1: 确认 `my9` 的真实路径与输入结构
- [x] 阶段2: 做 `my9` 的最小动态抽帧验证
- [ ] 阶段3: 完成 `my9` 的 `prepare`
- [ ] 阶段4: 完成 `my9` 的 `35000` 训练与视频/ply 导出
- [ ] 阶段5: 回写支线 `notes/WORKLOG`, 收尾交付

### 已执行动作
- 已启动 `my9 prepare`:
  - 会话 `94739`
  - `prepare_pid=817574`
- 已启动 `my9` 自动接力脚本:
  - 会话 `11201`
  - 脚本路径: `/tmp/fastgs_logs/my9_pipeline_20260328_1714.sh`
- 当前策略与 `my5` 到 `my8` 一致:
  - 等 `prepare` 结束
  - 校验 `images=324` 与 `sparse/0`
  - 再按 `1000` 步一段 guarded 训练到 `35000`
  - 最后自动 render + ffmpeg 封装

### 当前状态
- 当前仍在阶段3.
- 下一步继续观察 `my9 prepare` 是否顺利跨过抽帧、匹配和 `mapper`.

## [2026-03-28 17:16:23 UTC] [Session ID: 019d339f-290f-7032-8726-3095f62295ac] [记录类型]: `my9 prepare` 已跨过抽帧与 `feature_extractor`, 当前在 matcher

### 进度更新
- [x] 阶段1: 确认 `my9` 的真实路径与输入结构
- [x] 阶段2: 做 `my9` 的最小动态抽帧验证
- [ ] 阶段3: 完成 `my9` 的 `prepare`
- [ ] 阶段4: 完成 `my9` 的 `35000` 训练与视频/ply 导出
- [ ] 阶段5: 回写支线 `notes/WORKLOG`, 收尾交付

### 已验证结论
- `my9` 当前已完成:
  - `input=324`
  - `feature_extractor 324/324`
- `my9 prepare` 会话最近已推进到:
  - `Processing block [1/7, 3/7]`
- 自动接力会话仍健康等待, 没有丢链.

### 当前状态
- 当前仍在阶段3.
- 下一步继续等待 `exhaustive_matcher -> mapper -> 导出` 完成.
