# 任务计划: 支持 `my5` 多镜头视频目录走 `COLMAP -> FastGS`

## [2026-03-27 09:41:03] [Session ID: 28616] [记录类型]: 新任务建档

### 目标
- 让现有脚本支持 `/root/autodl-fs/my5` 这种“多视角目录 + 每视角生成视频 + 每视角 mask 视频”的输入结构.
- 先按 `cmd.md` 中 `my4_mask_guarded_v4` 的参数口径, 跑通一版 `COLMAP -> FastGS / 3DGS` 训练.
- 优先做最正确的入口改造, 不要求这轮就把所有调优都做完.

### 两种方向
- 方案A(不惜代价, 最佳):
  - 让 `convert.py` 直接识别 `generated_videos/*.mp4` 与配套 `merged_mask.mp4`.
  - 让 `run_lyra_colmap_fastgs.sh` 自动接住这类目录, 产出可直接训练的 `images + sparse/0 + masks`.
- 方案B(先能用, 后面再优雅):
  - 新增一个只面向 `my5` 的 wrapper 或预处理分支.
  - 先把 `generated_videos` 和 `merged_mask` 抽成帧图与 mask 图, 再复用现有训练入口.

### 阶段
- [x] 阶段1: 回读上下文, 做持续学习与续档
- [x] 阶段2: 核对 `my5` 目录结构与 `cmd.md` 参数口径
- [ ] 阶段3: 设计并实现输入发现 / mask 抽帧改造
- [ ] 阶段4: 用最小验证确认 `prepare` 产物正确
- [ ] 阶段5: 按 `my4_mask_guarded_v4` 参数启动训练并记录结果

### 关键问题
1. `/root/autodl-fs/my5` 里真正该给 COLMAP 的视频, 是不是只有 `*/generated_videos/generated_video_0.mp4`?
2. `rendering_4D_maps/merged_mask.mp4` 是否与 RGB 视频逐帧对齐, 能否直接抽成训练 mask?
3. 改动最稳的挂载点是在 `convert.py`, 还是只在 wrapper 层做结构归一化?
4. 如何保证多视角 RGB 帧名和 mask 帧名严格一一对应?

### 现象 -> 假设 -> 验证计划
- 现象:
  - `/root/autodl-fs/my5` 每个视角目录下同时存在:
    - `generated_videos/generated_video_0.mp4`
    - `rendering_4D_maps/merged_mask.mp4`
    - 以及多种深度 / 背景 / 4D map 视频
  - 现有 `convert.py` 会在缺少 `rgb/` 时退到全局递归发现视频.
- 当前主假设:
  - 如果不改发现规则, 现有流程会把 `depth` / `background` / `mask` 之类的视频一并送进 COLMAP, 导致输入脏掉.
- 备选解释:
  - 即便只抽对了 RGB 视频, 训练阶段如果没有同步准备 mask 图, 也无法真正按 `my4_mask_guarded_v4` 的口径开跑.
- 推翻主假设的证据:
  - 如果当前发现逻辑已经只会命中 `generated_videos`, 那就不需要改视频发现规则, 只要补 mask 入口.

### 当前状态
- 目前在阶段3.
- 下一步先实现:
  - `generated_videos` 优先发现
  - 可选的并行 mask 视频抽帧
  - wrapper 对 `my5` 的默认 mask 目录识别

## [2026-03-27 17:55:35] [Session ID: 28616] [记录类型]: 代码改造完成, 进入真实动态验证

### 进度更新
- [x] 阶段3: 设计并实现输入发现 / mask 抽帧改造
- [ ] 阶段4: 用最小验证确认 `prepare` 产物正确
- [ ] 阶段5: 按 `my4_mask_guarded_v4` 参数启动训练并记录结果

### 已完成的实现
- `convert.py`
  - 新增 `generated_videos` 优先发现, 避免误扫 `rendering_4D_maps/*.mp4`
  - 新增 VerseCrafter 风格 `merged_mask.mp4` 自动配对
  - 新增 RGB / mask 同步抽帧, 输出到:
    - `input/*.jpg`
    - `masks/*.png`
  - 新增 COLMAP 3.x / 4.x GPU 参数兼容层
- `scripts/run_lyra_colmap_fastgs.sh`
  - 训练阶段自动优先识别 `<fastgs-root>/masks`
  - 新增默认 COLMAP 路径缺失时回退到 PATH `colmap` 的逻辑
- 测试:
  - `tests/test_convert.py` 已补 3 条回归测试

### 动态证据
- 静态验证:
  - `python3 -m py_compile convert.py`
  - `bash -n scripts/run_lyra_colmap_fastgs.sh`
  - `pixi run python -m unittest tests.test_convert`
- 真实 `my5` 验证:
  - `prepare` 已成功走到:
    - `feature_extractor`
    - `exhaustive_matcher`
  - 已确认中间产物:
    - `input`: 972 张
    - `masks`: 972 张
    - `distorted/database.db`: 已生成, 当前约 289 MB

### 当前判断
- `my5` 这种多镜头生成视频目录已经被脚本正确识别.
- 当前剩余未完成项不再是“结构支持”, 而是等待真实 COLMAP prepare 跑完, 再接正式训练.

## [2026-03-27 17:56:30] [Session ID: 28616] [记录类型]: 已串联后台训练接力

### 新进展
- 已启动真实 `prepare` 会话, 当前已进入 `exhaustive_matcher`.
- 已额外启动训练接力:
  - 等 `data/my5_colmap_fastgs/sparse/0` 出现后
  - 自动按 `my4_mask_guarded_v4` 参数启动 `output/my5_mask_guarded_v1`

### 当前状态
- [x] 阶段4: 已有真实动态证据证明 prepare 主链路正在正常推进
- [ ] 阶段5: 正在等待 COLMAP 完成后自动进入正式训练

## [2026-03-27 10:20:15] [Session ID: 80800] [记录类型]: 用户纠正 `merged_mask.mp4` 语义后重订执行口径

### 新现象
- 用户明确说明: `rendering_4D_maps/merged_mask.mp4` 是“非深度数据区域”的 mask.
- 这个 mask 是给深度图链路用的, 不是 RGB / photometric 训练 mask.
- 当前后台 `prepare` 仍停留在 COLMAP `exhaustive_matcher`, 还没有进入 FastGS 训练.

### 当前主假设
- 之前把 `merged_mask.mp4` 自动抽成 `<fastgs-root>/masks` 的逻辑, 会把“深度辅助 mask”误接到训练 alpha mask 入口.
- 这不是参数问题, 而是输入语义判断错了.

### 备选解释
- 也可能这份 `merged_mask.mp4` 在某些后续深度辅助流程里有价值.
- 但就当前 FastGS 训练入口而言, 没有证据支持它应该接到 `--mask_dir` 或 `<scene_root>/masks`.

### 最小验证与执行计划
- [ ] 从默认代码路径里撤掉 `merged_mask.mp4 -> masks/` 的自动接线
- [ ] 更新脚本帮助与回归测试, 明确当前 `my5` 首轮训练默认无 mask
- [ ] 保留正在运行的 COLMAP prepare, 待其完成后清理错误生成的 `data/my5_colmap_fastgs/masks`
- [ ] 按 `cmd.md` 的 `my4_mask_guarded_v4` 参数口径启动一轮“无训练 mask”的 `my5` 训练

### 当前状态
- 目前在“纠正 mask 语义并回滚默认行为”这个子步骤.
- 接下来先改代码, 然后做静态与单测验证.

## [2026-03-27 10:27:38] [Session ID: 80800] [记录类型]: 默认行为回滚已完成, 已接上无 mask 训练等待器

### 进度更新
- [x] 从默认代码路径里撤掉 `merged_mask.mp4 -> masks/` 的自动接线
- [x] 更新脚本帮助与回归测试, 明确当前 `my5` 首轮训练默认无 mask
- [x] 保留正在运行的 COLMAP prepare, 并把误生成的 `data/my5_colmap_fastgs/masks` 挪出训练语义目录
- [ ] 按 `cmd.md` 的 `my4_mask_guarded_v4` 参数口径完成一轮“无训练 mask”的 `my5` 训练

### 验证结果
- 已通过:
  - `python3 -m py_compile convert.py scene/dataset_readers.py`
  - `bash -n scripts/run_lyra_colmap_fastgs.sh`
  - `pixi run python -m unittest tests.test_convert tests.test_mask_loading`
- 已执行的数据侧清理:
  - `data/my5_colmap_fastgs/masks`
  - -> `data/my5_colmap_fastgs/depth_masks_from_merged_mask_20260327_102621`
  - 当前保留了 `972` 张深度辅助 mask 帧, 但已不在训练自动识别路径里

### 后台任务
- 真实 COLMAP prepare:
  - 会话 `26742`
  - 当前仍在 `exhaustive_matcher`
- 无 mask 训练等待器:
  - 会话 `96836`
  - 待 `data/my5_colmap_fastgs/images` 与 `sparse/0` 就绪后, 自动启动:
    - `output/my5_nomask_v1`

### 当前状态
- 目前主线阻塞点已经从“代码口径错误”切换成“等待 COLMAP 真实完成”.
- 一旦 `prepare` 结束, 训练会自动接棒, 不再需要手动拼命令.

## [2026-03-27 12:13:40] [Session ID: 80800] [记录类型]: 用户要求将 `my5` 图量降到原来的 1/3

### 新现象
- 用户认为当前 `972` 张图过多.
- 新要求是: 改用原图量的 `1/3`.

### 当前主假设
- `my5` 每视角当前是 `81` 帧, 共 `12` 视角, 总计 `972` 张.
- 若要降到 `1/3`, 目标应是:
  - 每视角 `27` 帧
  - 总计 `324` 张
- 对应抽帧率从 `16 fps` 改成 `16 / 3 = 5.333333333333 fps`.

### 最小验证与执行计划
- [ ] 用单个视角视频验证 `--video-fps 5.333333333333` 是否落到 `27` 帧
- [ ] 停掉当前 `prepare` 与训练等待器
- [ ] 按新抽帧率重启 `COLMAP prepare`
- [ ] 重新挂上无 mask 训练等待器, 保持其余训练参数不变

### 当前状态
- 目前切换到“缩减 `my5` 图量并重跑”的子任务.

## [2026-03-27 12:16:04] [Session ID: 80800] [记录类型]: `my5` 已切换到 1/3 图量并重启 prepare

### 进度更新
- [x] 用单个视角视频验证 `--video-fps 5.333333333333` 会落到 `27` 帧
- [x] 停掉旧的 `prepare` 与训练等待器
- [x] 修掉 `run_lyra_colmap_fastgs.sh` 在符号链接工作目录下的 `--overwrite` 路径误判
- [x] 按新抽帧率重启 `COLMAP prepare`
- [x] 重新挂上无 mask 训练等待器

### 动态证据
- 单视角最小验证:
  - `ffmpeg ... -vf fps=5.333333333333 ...`
  - 输出: `count=27`
- 新 prepare 会话:
  - 会话 `23869`
  - 抽帧日志已出现多次 `frame=   27`
  - `feature_extractor` 已处理到 `Processed file [324/324]`
  - 当前已进入新的 `feature matching`:
    - `Processing block [1/7, 1/7]`
- 新训练等待器:
  - 会话 `53980`
  - 等 `images + sparse/0` 就绪后自动启动 `output/my5_nomask_v1`

### 当前状态
- `my5` 当前图量口径已经从 `972` 张切到 `324` 张.
- 主线正在新的 1/3 图量 COLMAP prepare 上推进.

## [2026-03-27 12:58:00] [Session ID: 80800] [记录类型]: `my5` 1/3 图量最新进度回报

### 当前动态证据
- 新 `prepare` 会话 `23869` 已完成:
  - `feature matching`
  - 总耗时约 `37.506` 分钟
- 当前已经进入 `mapper`.
- 最新可见注册进度:
  - `num_reg_frames=115`
- 当前数据目录状态:
  - `input = 324`
  - `distorted/database.db = 265601024 bytes`
  - `images` / `sparse/0` 仍未产出
- 训练等待器 `53980` 仍在等待 `images + sparse/0` 就绪.

### 当前状态
- 目前已经不在匹配阶段.
- 现在主线在 COLMAP `mapper` 注册图像阶段推进, 训练还没开始.

## [2026-03-27 13:40:48] [Session ID: 80800] [记录类型]: `my5` 1/3 图量 prepare 已完成, 首轮训练在 21000 附近中断

### 当前动态证据
- `prepare` 会话 `23869` 已完成全部流程:
  - `feature_matching`
  - `mapper`
  - `image_undistorter`
- 已确认稀疏重建结果:
  - `registered_images=324`
  - `points=30359`
- 当前数据目录状态:
  - `images = 324`
  - `sparse/0 = 6` 个文件
- 训练等待器 `53980` 已自动接棒启动 `output/my5_nomask_v1`.
- 训练动态进度:
  - 已保存 `ITER 10000`
  - 已保存 `ITER 20000`
  - 在约 `ITER 21000` 处报错退出
- 当前错误现象:
  - `torch.AcceleratorError: CUDA error: an illegal memory access was encountered`

### 当前状态
- `my5` 的 COLMAP prepare 已经完成.
- 首轮无 mask 训练已经跑起来, 但目前卡在 `21000` 左右的 CUDA 非法访问.
- 下一步应从已保存的 `20000` checkpoint 续训, 或先做最小定位后再续.

## [2026-03-27 13:43:30] [Session ID: 80800] [记录类型]: 用户同意从 `20000` checkpoint 继续推进 `my5` 到 `30000`

### 现象
- `prepare` 已完整结束, 稀疏重建与 undistort 产物都已落地.
- 首轮训练已保存:
  - `ckpt_10000.pth`
  - `ckpt_20000.pth`
- 训练在约 `21000` 附近因 `CUDA illegal memory access` 退出.

### 当前主假设
- 这次失败属于仓库里已经出现过的“训练后段随机 CUDA 非法访问”同类问题.
- 当前最优先且最小扰动的推进方式, 不是立刻改核心 CUDA 代码, 而是:
  - 从 `20000` checkpoint 续训
  - 按 `1000` 步分段
  - 每段失败时换 seed 重试

### 最强备选解释
- 也可能 `my5` 这组数据在 `21000+` 存在确定性的模型状态问题, 导致不管换多少 seed 都会在同一段崩.
- 如果连续多个 seed 都无法把 `20000 -> 21000` 推过去, 就需要回滚当前假设, 转去做更小粒度定位.

### 最小验证与执行计划
- [ ] 核对 `ckpt_20000.pth` 与现有模型目录状态
- [ ] 启动 guarded resume, 先试 `20000 -> 21000`
- [ ] 若首段成功, 继续逐段推进到 `30000`
- [ ] 完成后再决定是否立刻补 render / evaluate

### 当前状态
- 目前切换到“从 `20000` checkpoint 继续守护续训”的子任务.

## [2026-03-27 21:50:00] [Session ID: 245310] [记录类型]: `my5` 续训阻塞点从随机 CUDA 崩溃收敛到稳定的 resume 属性缺失

### 新现象
- `output/my5_nomask_v1/checkpoints/ckpt_21000.pth` 已经成功生成.
- `output/my5_nomask_v1/point_cloud/iteration_21000/point_cloud.ply` 也已存在.
- guarded resume 会话 `42157` 连续多次都能稳定跑完 `20000 -> 21000`.
- 真实报错点一致:
  - `train.py:182`
  - `scene/gaussian_model.py:540`
  - `scene/gaussian_model.py:380`
  - `AttributeError: 'GaussianModel' object has no attribute 'tmp_radii'`

### 当前主假设
- 这次 resume 失败不是训练随机性导致.
- 更像是 `GaussianModel` 在 checkpoint restore 路径下没有把 `tmp_radii` 初始化出来.
- 非 resume 首轮训练之所以没在更早处报这个错, 说明 `tmp_radii` 在正常训练流里会被别的路径建立.

### 最强备选解释
- 也可能不是 `restore()` 漏恢复, 而是 `prune_points()` / `final_prune_fastgs()` 假设了某个只在训练前向中临时创建的字段一定存在.
- 如果代码阅读后发现 `tmp_radii` 从来就不该进 checkpoint, 那更稳的修法应是给缺失字段补安全初始化, 而不是盲目塞进 restore payload.

### 最小验证与执行计划
- [ ] 读 `train.py` 和 `scene/gaussian_model.py`, 定位 `tmp_radii` 的定义、赋值、capture/restore 关系
- [ ] 做最小代码修复, 让 resume 路径不再在 `final_prune_fastgs` 崩溃
- [ ] 做静态校验和最小 resume 验证, 优先验证 `21000 -> 22000`
- [ ] 若通过, 再继续 guarded 方式推向 `30000`

### 当前状态
- 目前进入“resume 路径属性缺失修复”子任务.

## [2026-03-27 21:59:00] [Session ID: 245310] [记录类型]: `my5` resume 修复完成, 训练已推进到 `30000`

### 进度更新
- [x] 读 `train.py` 和 `scene/gaussian_model.py`, 定位 `tmp_radii` 的定义、赋值、capture/restore 关系
- [x] 做最小代码修复, 让 resume 路径不再在 `final_prune_fastgs` 崩溃
- [x] 做静态校验和最小 resume 验证, 已验证 `20000 -> 21000` 可完整退出
- [x] 重新 guarded 续训到 `30000`

### 已验证结论
- resume 阻塞点已经从“候选的随机 CUDA 问题”收敛成“稳定的 `tmp_radii` 属性缺失”.
- 最小修复后, 同一路径 `20000 -> 21000` 已能成功经过 `final_prune_fastgs` 并正常结束.
- 后续 `21000 -> 30000` 逐段续训全部一次成功, 无需换 seed 重试.

### 当前状态
- `output/my5_nomask_v1/checkpoints/ckpt_30000.pth` 已生成.
- `output/my5_nomask_v1/point_cloud/iteration_30000/point_cloud.ply` 已生成.
- 本轮“按 1/3 图量跑通 `COLMAP -> FastGS` 并完成 30000 训练”的主目标已完成.

## [2026-03-27 14:33:01 UTC] [Session ID: 277426] [记录类型]: 使用 `GITHUB_TOKEN` 推送当前 `main` 的已提交提交

### 新现象
- 用户要求使用 `GITHUB_TOKEN` 执行推送.
- 当前仓库状态已确认:
  - `main` 相对 `origin/main` 超前 `1` 个提交
  - 工作区仍有未提交改动, 但它们不会随本次 push 一起进入远端
  - `GITHUB_TOKEN` 已存在于当前环境
  - `git submodule status --recursive` 为空, 当前仓库没有需要同步的子模块指针

### 当前主假设
- 用户这次要做的是把当前已经存在的本地提交 `122ba55` 推到 `origin/main`.
- 更稳的做法是:
  - 不改动工作区
  - 不临时改写远端 URL
  - 直接用一次性的 HTTP 认证头携带 `GITHUB_TOKEN` 完成推送

### 最强备选解释
- 也可能远端仓库对当前 token 权限不足, 或 token 只能用于 GitHub Actions 场景.
- 如果 push 返回 `403` / `401`, 就说明“token 已存在”不等于“token 对这个仓库具备写权限”.

### 最小验证与执行计划
- [ ] 先核对待推送提交与 `origin/main` 的差异
- [ ] 使用一次性认证头执行 `git push origin HEAD:main`
- [ ] 读取返回结果, 确认远端分支是否已到达 `122ba55`

### 当前状态
- 目前进入“仅推送已提交提交, 不包含工作区未提交改动”的子任务.

## [2026-03-27 14:33:48 UTC] [Session ID: 277426] [记录类型]: 首轮 `GITHUB_TOKEN` 推送失败, 转入认证有效性排查

### 已观察到的现象
- 使用一次性 HTTP 认证头执行:
  - `git push origin HEAD:main`
- GitHub 返回的动态错误是:
  - `remote: Invalid username or token. Password authentication is not supported for Git operations.`
- 同时环境中的 `GITHUB_TOKEN` 具备这些静态特征:
  - 前缀是 `github_pat_`
  - 长度为 `94`

### 当前主假设
- token 本身可能无效, 已过期, 或缺少对 `raiscui/FastGS` 的写权限.
- 也不排除第一轮认证拼接方式与 GitHub 当前接受的凭据格式存在偏差.

### 最强备选解释
- 本机 Git 的凭据辅助链路里还残留了失效的 VS Code 凭据管道.
- 如果后续换成显式 URL 凭据或更强制的禁用 credential helper 方式后成功, 就说明上一轮失败并不能证明 token 本身无效.

### 最小验证与执行计划
- [ ] 用无交互方式验证公开远端连通性, 排除网络问题
- [ ] 用 GitHub API 最小验证 token 是否有效
- [ ] 若 token 有效, 再换一种不依赖 helper 的 Git 认证方式重试 push
- [ ] 若 token 无效或无权限, 明确给出失败原因和下一步需要的权限口径

### 当前状态
- 目前已从“直接推送”切换到“认证有效性与权限验证”.

## [2026-03-27 14:35:06 UTC] [Session ID: 277426] [记录类型]: 新增代理干扰候选假设, 先做无代理最小验证

### 已观察到的现象
- 当前环境存在:
  - `http_proxy=http://127.0.0.1:7897`
  - `https_proxy=http://127.0.0.1:7897`
  - `all_proxy=socks5://127.0.0.1:7897`
- 用 `curl` 访问 GitHub API 时出现:
  - `HTTP/1.1 200 Connection established`
  - 随后是 `400 Bad Request`
- 这说明 API 验证链路目前被本机代理显著影响.

### 当前主假设
- 前两次 `git push` 的失败仍然更像认证问题.
- 但由于当前代理已经确认会污染 GitHub API 请求, 还需要补一个“无代理”的最小实验, 才能把“代理篡改认证链路”排除干净.

### 最强备选解释
- 如果无代理后 `git push` 或 GitHub API 验证立刻恢复正常, 那前面把责任全部归到 token 本身就是不成立的.

### 最小验证与执行计划
- [ ] 先在无代理环境下验证 GitHub API / Git 远端连通性
- [ ] 若无代理链路正常, 直接在无代理环境下重试 `git push`
- [ ] 若仍失败, 再把结论收敛到 token 无效或权限不足

### 当前状态
- 下一步先做“去代理后的最小认证验证”.

## [2026-03-27 14:36:08 UTC] [Session ID: 277426] [记录类型]: 认证头非法的动态证据已拿到, 主假设切换为“环境变量尾部污染”

### 已观察到的现象
- 使用 `python3 urllib` 直接构造 GitHub API 请求时, 没有再经过 shell 引号拼接.
- Python 报出的真实异常是:
  - `ValueError: Invalid header value`
- 关键动态证据:
  - 认证头里的 token 结尾包含隐藏的 `\\r`

### 当前主假设
- 当前 `GITHUB_TOKEN` 本身很可能是对的.
- 但它在环境变量里被带上了结尾回车, 导致:
  - GitHub API 认证头非法
  - Git push 基本认证 payload 也随之失效

### 最强备选解释
- 即使去掉 `\\r` 之后, token 仍可能已经失效或没有写权限.
- 如果清洗后的 token 仍然被 GitHub 拒绝, 就需要把主假设回滚到“token 本身无效或权限不足”.

### 最小验证与执行计划
- [ ] 先在当前 shell 内构造一个去掉 `\\r` / `\\n` 的 clean token
- [ ] 用 clean token 做一次最小 GitHub 认证验证
- [ ] 若认证通过, 立即用 clean token 执行无代理 `git push`
- [ ] 推送成功后再核对 `origin/main` 是否到达 `122ba55`

### 当前状态
- 目前进入“清洗环境变量尾部污染后重试认证”的子任务.

## [2026-03-27 14:37:06 UTC] [Session ID: 277426] [记录类型]: `GITHUB_TOKEN` 推送完成, 根因确认是尾部回车污染

### 进度更新
- [x] 核对待推送提交与 `origin/main` 的差异
- [x] 找到首轮推送失败的真实阻塞点
- [x] 清洗 `GITHUB_TOKEN` 末尾的 `\\r` / `\\n`
- [x] 使用 clean token 执行无代理 `git push origin HEAD:main`
- [x] 核对远端 `refs/heads/main` 已到达 `122ba55`

### 已验证结论
- 这次失败不是仓库权限缺失.
- 动态证据已证明:
  - 清洗后的 token 可以成功访问 GitHub API
  - `/repos/raiscui/FastGS` 返回 `push: true`
  - 实际 `git push` 已成功:
    - `ebe06ac..122ba55  HEAD -> main`
  - 远端校验结果:
    - `refs/heads/main = 122ba55e3ebec97f70ab27c098a88e3441c26ac8`
- 因此本次真实根因是:
  - 环境变量 `GITHUB_TOKEN` 尾部混入了回车符 `\\r`, 导致认证头非法

### 当前状态
- 用户要求的“使用 `GITHUB_TOKEN` 推送”已经完成.
- 注意:
  - 这次推上去的是已存在的本地提交 `122ba55`
  - 当前工作区里后来追加的 `task_plan.md` 等未提交改动, 并没有随这次 push 一起进入远端

## [2026-03-27 22:03:00] [Session ID: 245310] [记录类型]: 导出 `my5_nomask_v1` 的渲染视频

### 新现象
- 用户当前要求直接输出 `my5_nomask_v1` 的视频.
- 模型训练已经结束, 现在需要走 `render.py + ffmpeg` 导出 mp4.

### 最小执行计划
- [ ] 用现有 wrapper 的 `render + video export` 链路导出 `30000` 的 train/test 视频
- [ ] 核查 `videos/*.mp4` 是否成功落盘
- [ ] 向用户回报输出路径

### 当前状态
- 目前进入“渲染并导出 `my5_nomask_v1` 视频”子任务.

## [2026-03-27 22:36:00] [Session ID: 245310] [记录类型]: `my5_nomask_v1` 视频导出完成

### 进度更新
- [x] 用现有 wrapper 的 `render + video export` 链路导出 `30000` 的 train/test 视频
- [x] 核查 `videos/*.mp4` 已成功落盘
- [x] 可向用户回报输出路径

### 当前状态
- `output/my5_nomask_v1/videos/train_iter30000.mp4` 已生成
- `output/my5_nomask_v1/videos/test_iter30000.mp4` 已生成

## [2026-03-27 22:38:00] [Session ID: 245310] [记录类型]: 评估 `my5_nomask_v1`

### 新现象
- `my5_nomask_v1` 的 `30000` 轮 train/test 渲染与 mp4 已经完成.
- 用户当前要求对这轮结果做正式评估.

### 最小执行计划
- [ ] 复用现有 metrics 链路计算 `PSNR / SSIM / LPIPS`
- [ ] 核查 `results.json` 与 `per_view.json` 是否成功落盘
- [ ] 汇总关键指标并回报用户

### 当前状态
- 目前进入“评估 `my5_nomask_v1` 渲染结果”子任务.

## [2026-03-27 22:39:10] [Session ID: 245310] [记录类型]: `my5_nomask_v1` 评估完成

### 进度更新
- [x] 复用现有 metrics 链路计算 `PSNR / SSIM / LPIPS`
- [x] 核查 `results.json` 与 `per_view.json` 已成功落盘
- [x] 可向用户汇总关键指标

### 当前状态
- `output/my5_nomask_v1/results.json` 已生成
- `output/my5_nomask_v1/per_view.json` 已生成
- 当前 `30000` 轮 test 指标已落地, 可作为后续调优基线

## [2026-03-27 22:41:00] [Session ID: 245310] [记录类型]: 将 `my5_nomask_v1` 从 `30000` 扩展到 `35000` 并重新评估

### 新现象
- 用户要求在当前 `my5_nomask_v1` 基础上继续跑到 `35000`, 并沿用当前这组训练参数口径:
  - `-r 1`
  - `--densification_interval 500`
  - `--opacity_reset_interval 3000`
  - `--densify_until_iter 15000`
  - `--position_lr_max_steps 35000`
  - `--loss_thresh 0.1`
  - `--grad_thresh 0.0002`
  - `--grad_abs_thresh 0.0012`
  - `--highfeature_lr 0.005`
  - `--lowfeature_lr 0.0025`
  - `--dense 0.001`
  - `--mult 0.5`
  - `--optimizer_type default`
  - `--eval`
- 当前已有稳定基线:
  - `30000` 训练完成
  - `30000` 视频已导出
  - `30000` test 指标已落地

### 两种执行方向
- 方案A(更稳):
  - 从 `ckpt_30000.pth` 分段 guarded 续训到 `35000`
  - 每 `1000` 步保存一次 checkpoint
  - 完成后再 render + metrics
- 方案B(更快):
  - 从 `ckpt_30000.pth` 一次性直接跑到 `35000`
  - 成功后再 render + metrics

### 当前决定
- 本轮直接采用方案A.
- 原因:
  - 这台机器和这份模型已经出现过 resume / 后段稳定性问题
  - 分段 guarded 更稳, 也更容易保留中间检查点

### 最小执行计划
- [ ] 核查 `ckpt_30000.pth` 与当前模型状态
- [ ] 以现有参数口径 guarded 续训 `30000 -> 35000`
- [ ] 对 `35000` 执行 render + metrics
- [ ] 汇总 `35000` 指标并与 `30000` 基线对比

### 当前状态
- 目前进入“`35000` 续训与评估”子任务.

## [2026-03-27 22:44:04] [Session ID: 366184] [记录类型]: `my5_nomask_v1` 已续训到 `35000`, 转入正式评估

### 新现象
- `output/my5_nomask_v1/checkpoints/ckpt_35000.pth` 已落盘.
- `output/my5_nomask_v1/point_cloud/iteration_35000/point_cloud.ply` 已落盘.
- guarded 日志确认最后一段 `34000 -> 35000` 已成功结束:
  - `Training complete.`
  - `segment 34000->35000 success with seed=35101`

### 已验证结论
- 这轮按用户要求扩到 `35000` 的分段续训已经完成.
- 当前不是继续等训练, 而是应立即执行 `35000` 的 render + metrics, 固化客观结果.

### 进度更新
- [x] 阶段5: 按用户给定的 `35000` 调参口径完成续训
- [ ] 阶段6: 对 `35000` 轮执行 render / metrics 并与 `30000` 对比

### 当前状态
- 目前在阶段6.
- 下一步直接运行:
  - `bash scripts/run_lyra_colmap_fastgs.sh --phase evaluate --model-path output/my5_nomask_v1 --iteration 35000 --overwrite`

## [2026-03-27 22:46:48] [Session ID: 019d2ea0-a35d-7a12-b8e9-9ebec510ea80] [记录类型]: `35000` 评估完成, 当前进入结果解读

### 进度更新
- [x] 阶段6: 对 `35000` 轮执行 render / metrics 并与 `30000` 对比

### 已验证结论
- `35000` 平均 test 指标:
  - `PSNR 27.2039413`
  - `SSIM 0.8910136`
  - `LPIPS 0.2026276`
- 相比 `30000`:
  - `PSNR +0.0402832`
  - `SSIM +0.0004694`
  - `LPIPS -0.0006576`
- 当前这轮“扩到 35000”已经完整闭环, 包括:
  - 续训
  - render
  - metrics
  - 结果比对

### 当前状态
- 主目标已完成.
- 下一步如果继续, 更合理的方向是围绕 worst-view 和 COLMAP 位姿质量做针对性调优, 而不是盲目继续加迭代数.

## [2026-03-27 22:48:30] [Session ID: 019d2ea0-a35d-7a12-b8e9-9ebec510ea80] [记录类型]: 启动 `my5_nomask_v1` 的 worst-view 定位报告

### 目标
- 针对 `ours_35000` 的最差 test 视角做定位报告.
- 不只给平均指标, 还要说明最差视角到底差在哪里, 更像哪一类问题.

### 最小执行计划
- [ ] 从 `per_view.json` 抽取最差视角名单
- [ ] 对齐对应的 GT / render / 相机信息 / 误差图
- [ ] 结合 COLMAP / 相机分布判断更像位姿问题还是素材问题
- [ ] 产出 markdown 报告并给出下一步建议

### 当前状态
- 目前进入 worst-view 定位子任务.
- 下一步先提取 `ours_35000` 的 worst views, 再逐张核查图像与相机信息.

## [2026-03-27 22:59:40] [Session ID: 019d2ea0-a35d-7a12-b8e9-9ebec510ea80] [记录类型]: worst-view 定位报告已完成

### 进度更新
- [x] 从 `per_view.json` 抽取最差视角名单
- [x] 对齐对应的 GT / render / 相机信息 / 误差图
- [x] 结合 COLMAP / 相机分布判断更像位姿问题还是素材问题
- [x] 产出 markdown 报告并给出下一步建议

### 已验证结论
- 正式报告已落盘:
  - `specs/my5_worst_view_report_20260327.md`
- 图像资产已落盘:
  - `specs/my5_worst_view_report_assets/`
- mermaid 图表已用 `beautiful-mermaid-rs` 校验通过.

### 当前状态
- 当前子任务已完成.
- 下一步如果继续, 建议先回查 `view 7 / 5 / 8` 的 COLMAP 位姿轨迹.

## [2026-03-28 00:00:40] [Session ID: 019d2ea0-a35d-7a12-b8e9-9ebec510ea80] [记录类型]: 启动 `view 7 / 5 / 8` 的 COLMAP 位姿轨迹回查

### 目标
- 回查 `my5_nomask_v1` worst-view 对应机位在 COLMAP 中的相机中心与朝向.
- 进一步判断当前更像材质难点, 还是少数机位存在姿态异常.

### 最小执行计划
- [ ] 读取 `data/my5_colmap_fastgs/sparse/0` 的 COLMAP 模型
- [ ] 映射 `view 7 / 5 / 8` 与相邻机位的相机轨迹
- [ ] 产出轨迹可视化与异常度量
- [ ] 写定位报告并给出下一步建议

### 当前状态
- 目前进入 COLMAP 位姿回查子任务.
- 下一步先确认仓库里是否已有 COLMAP 模型读取工具, 再决定复用还是临时分析脚本.

## [2026-03-28 01:46:16 UTC] [Session ID: 019d2fcd-edfd-7032-be9f-42f8bc79198c] [记录类型]: 导出 `my5_nomask_v1` 的 `35000` 轮视频

### 新现象
- 用户当前请求是“出 `35000` 的视频”.
- 已有静态证据表明:
  - `output/my5_nomask_v1/checkpoints/ckpt_35000.pth` 已存在
  - `output/my5_nomask_v1/train/ours_35000/renders` 已存在
  - `output/my5_nomask_v1/test/ours_35000/renders` 已存在
- 当前还没有看到 `output/my5_nomask_v1/videos/train_iter35000.mp4` 与 `output/my5_nomask_v1/videos/test_iter35000.mp4` 的落盘证据.

### 最小执行计划
- [ ] 复用 `scripts/run_lyra_colmap_fastgs.sh` 导出 `35000` 的 train/test 视频
- [ ] 核查 mp4 文件是否成功生成
- [ ] 记录本轮交付结果并回复用户

### 当前状态
- 当前进入“`35000` 视频导出”子任务.
- 下一步直接执行 wrapper:
  - `bash scripts/run_lyra_colmap_fastgs.sh --phase render --model-path output/my5_nomask_v1 --video-iterations 35000 --video-sets both`

## [2026-03-28 01:48:27 UTC] [Session ID: 019d2fcd-edfd-7032-be9f-42f8bc79198c] [记录类型]: `35000` 视频导出完成

### 进度更新
- [x] 复用 `scripts/run_lyra_colmap_fastgs.sh` 导出 `35000` 的 train/test 视频
- [x] 核查 mp4 文件是否成功生成
- [x] 记录本轮交付结果并回复用户

### 已验证结论
- 已生成:
  - `output/my5_nomask_v1/videos/train_iter35000.mp4`
  - `output/my5_nomask_v1/videos/test_iter35000.mp4`
- 动态验证结果:
  - 两个文件都能被 `ffprobe` 正常识别
  - 编码为 `h264`
  - 分辨率为 `1280x720`
  - 帧率为 `24 fps`

### 当前状态
- 当前子任务已完成.
- 如需继续, 更自然的下一步是直接对比 `30000` 与 `35000` 的 train/test 视频观感差异.

## [2026-03-28 00:10:40] [Session ID: 019d2ea0-a35d-7a12-b8e9-9ebec510ea80] [记录类型]: COLMAP 位姿回查已完成

### 进度更新
- [x] 读取 `data/my5_colmap_fastgs/sparse/0` 的 COLMAP 模型
- [x] 映射 `view 7 / 5 / 8` 与相邻机位的相机轨迹
- [x] 产出轨迹可视化与异常度量
- [x] 写定位报告并给出下一步建议

### 已验证结论
- 正式报告已落盘:
  - `specs/my5_colmap_pose_report_20260328.md`
- 当前最强 pose 可疑点是 `view 7`.
- `view 5` 次之.
- `view 8` 更像材质 / 反射表达难点.

### 当前状态
- 当前子任务已完成.
- 下一步如果继续, 建议优先做 `view 7` 的更硬姿态连续性检查或局部 COLMAP 对照.

## [2026-03-28 02:05:00 UTC] [Session ID: 79642ac4-ccdf-404a-967b-1342f85cc2bd] [记录类型]: 启动 `view 7 / 5 / 8` 朝向连续性与跨机位相对位姿复核

### 目标
- 在上一轮“相机中心轨迹 + 点支持”之外, 补一层更硬的朝向连续性证据.
- 判断 `view 7` 的异常更像:
  - 真正的姿态跳变
  - 还是仅仅因为高反射 / 弱纹理导致点支持差.

### 两种方向
- 方案A(不惜代价, 最佳):
  - 读取 `images.bin` 的旋转矩阵, 统计 `view 7 / 5 / 8` 的 forward / up / right 向量连续性.
  - 计算邻帧旋转角、角速度, 再和相邻机位做相对位姿对照.
  - 生成图表和正式报告, 给出“现象 -> 假设 -> 验证 -> 结论”.
- 方案B(先能用, 后面再优雅):
  - 只做 `view 7 / 5 / 8` 的 forward 向量和邻帧旋转角.
  - 先判断 `view 7` 是否明显偏离, 再决定要不要补跨机位对照.

### 最小执行计划
- [ ] 复用现有 COLMAP 读取工具, 导出 `view 7 / 5 / 8` 的朝向向量与邻帧旋转角
- [ ] 补做与相邻机位的相对位姿连续性统计
- [ ] 生成图表与数据摘要
- [ ] 写报告并更新主线结论

### 当前状态
- 目前进入“更硬 pose 证据复核”子任务.
- 下一步先定位上一轮分析脚本或现有读取工具, 尽量复用, 避免临时脚本和正式报告口径脱节.

## [2026-03-28 02:24:00 UTC] [Session ID: 79642ac4-ccdf-404a-967b-1342f85cc2bd] [记录类型]: `view 7 / 5 / 8` 朝向连续性与相对位姿复核完成

### 进度更新
- [x] 复用现有 COLMAP 读取工具, 导出 `view 7 / 5 / 8` 的朝向向量与邻帧旋转角
- [x] 补做与相邻机位的相对位姿连续性统计
- [x] 生成图表与数据摘要
- [x] 写报告并更新主线结论

### 已验证结论
- 新报告已落盘:
  - `specs/my5_pose_continuity_report_20260328.md`
- 新资产已落盘:
  - `specs/my5_pose_continuity_report_assets/pose_continuity_data.json`
  - `specs/my5_pose_continuity_report_assets/orientation_global_scatter.png`
  - `specs/my5_pose_continuity_report_assets/focus_view_orientation_diagnostics.png`
  - `specs/my5_pose_continuity_report_assets/neighbor_relative_pose_diagnostics.png`
  - `specs/my5_pose_continuity_report_assets/focus_view_forward_quiver.png`
- `view 7` 现在不只是在“相机中心轨迹”上最可疑.
- 它在“朝向连续性”上也同样是全机位最差:
  - `rotation_mean = 3.6893`
  - `rotation_max = 6.0948`
  - `forward_mean = 3.6831`
  - `forward_max = 6.0883`
- 邻机位 pair 里:
  - `7-8` 的 `delta_relative_rotation_mean = 2.1338`, 全局最高
  - `6-7` 的 `delta_relative_rotation_mean = 1.9313`, 全局第二
- 当前更稳的判断是:
  - `view 7` 有真实 pose 连续性异常证据
  - `view 5` 是次级可疑点
  - `view 8` 更像材质 / 反射难点
  - 但 `view 7` 仍可能和高反射 / 弱纹理难点叠加, 不是单一根因已确认

### 当前状态
- 当前这个“更硬 pose 证据复核”子任务已完成.
- 下一步如果继续, 最值得优先做的是:
  - 只围绕 `view 6 / 7 / 8` 与 `frame 1 ~ 8` 做局部 COLMAP 对照

## [2026-03-28 02:34:00 UTC] [Session ID: 7f1d2edd-4a39-4aee-b0fd-79f9701c57e7] [记录类型]: 启动 `view 6 / 7 / 8 + frame 1~8` 的局部 COLMAP 对照实验

### 目标
- 用最小局部子集复现并对照 `view 7` 前段的位姿不稳定现象.
- 判断这段异常更像:
  - 全局重建把局部解坏了
  - 还是局部子集本身就很难稳定重建.

### 两种方向
- 方案A(不惜代价, 最佳):
  - 从原始抽帧图里只抽 `view 6 / 7 / 8` 的 `frame 1 ~ 8`.
  - 单独跑一次完整 COLMAP, 再和全局模型中同一批图的轨迹 / 朝向连续性逐项对照.
- 方案B(先能用, 后面再优雅):
  - 先把这 24 张图抽出来做一次最小局部 SfM.
  - 只看:
    - 是否能稳定注册
    - `view 7` 的前段 `step / rotation_step` 是否明显下降
  - 如果局部已经改善, 再决定要不要补更大窗口.

### 最小执行计划
- [ ] 核对当前全局重建的相机模型 / COLMAP 参数口径
- [ ] 准备 `view 6 / 7 / 8 + frame 1 ~ 8` 的局部图像子集
- [ ] 跑一版局部 COLMAP 重建
- [ ] 把局部结果和全局结果做指标对照
- [ ] 写报告并给出下一步建议

### 当前状态
- 当前进入“局部 COLMAP 对照实验”子任务.
- 下一步先确认全局模型的相机模型和当前 wrapper 的 COLMAP 参数, 避免局部实验因为口径偏了而不可比.

## [2026-03-28 02:49:00 UTC] [Session ID: 7f1d2edd-4a39-4aee-b0fd-79f9701c57e7] [记录类型]: `view 6 / 7 / 8 + frame 1~8` 局部 COLMAP 对照实验完成

### 进度更新
- [x] 核对当前全局重建的相机模型 / COLMAP 参数口径
- [x] 准备 `view 6 / 7 / 8 + frame 1 ~ 8` 的局部图像子集
- [x] 跑一版局部 COLMAP 重建
- [x] 把局部结果和全局结果做指标对照
- [x] 写报告并给出下一步建议

### 已验证结论
- 正式报告已落盘:
  - `specs/my5_local_colmap_compare_20260328.md`
- 对照资产已落盘:
  - `specs/my5_local_colmap_compare_assets/comparison_summary.json`
  - `specs/my5_local_colmap_compare_assets/view7_global_vs_local_compare.png`
  - `specs/my5_local_colmap_compare_assets/pair_global_vs_local_compare.png`
- 两版局部实验:
  - `local_fix`
  - `local_refine`
  都成功注册 `24/24`, 但都没有把 `view 7` 前段明显修顺
- `local_refine` 虽然把 focal 从 `592.17` 调到 `571.64`, 但:
  - `view 7 rot_mean` 只小幅下降
  - `view 7 step_mean` 仍明显高于全局
  - 点支持显著更低
- 当前更稳的判断是:
  - 这轮对照没有支持“全局重建上下文是主要问题”这个假设
  - `view 7` 更像局部素材本身就难, 或者局部约束本身不足

### 当前状态
- 当前“局部 COLMAP 对照实验”子任务已完成.
- 下一步如果继续, 更值得优先做的是:
  - `view 7 frame 3 ~ 6` 的筛帧对照
  - 而不是继续缩小 COLMAP 局部窗口或继续堆训练步数

## [2026-03-28 02:56:00 UTC] [Session ID: 7f1d2edd-4a39-4aee-b0fd-79f9701c57e7] [记录类型]: 放大局部 COLMAP 上下文窗口到 `view 5 / 6 / 7 / 8 + frame 1~12`

### 目标
- 进一步区分:
  - `24` 张局部窗口是不是约束太少
  - 还是 `view 7` 这段本身就难, 即便加上下文也不容易修顺

### 最小执行计划
- [ ] 准备 `view 5 / 6 / 7 / 8 + frame 1 ~ 12` 的扩大窗口子集
- [ ] 跑一版允许 focal refine 的局部 COLMAP
- [ ] 继续用同口径朝向分析脚本做对照
- [ ] 判断扩大上下文后 `view 7` 是否出现实质改善

### 当前状态
- 目前进入“扩大局部上下文窗口”子任务.
- 下一步直接准备 `48` 张图的对照子集, 避免先做会破坏连续性指标的删帧实验.

## [2026-03-27 18:50:01 UTC] [Session ID: ebb3c562-1702-4ea2-8e98-ed18d0a9bada] [记录类型]: 续接 `v3_widecontext` 状态核查并准备做结果归档

### 新现象
- 上一轮摘要里记录的 `v3_widecontext` mapper 持续 BA 数值失败循环, 当时还没有落盘 `sparse/0`.
- 当前重新核查后:
  - 相关 `timeout` / `colmap mapper` 进程已经不在运行.
  - `data/my5_local_pose_compare_v3_widecontext/distorted/sparse/0` 已经存在:
    - `cameras.bin`
    - `images.bin`
    - `points3D.bin`
    - 以及配套 `frames.bin / rigs.bin / project.ini`

### 当前主假设
- 这个大窗口实验有两种可能:
  - 可能是在多轮 BA 数值失败后, 仍然勉强收敛并写出了可读模型.
  - 也可能只是输出了一个形式上完整, 但质量并不可靠的残留模型.

### 最强备选解释
- 也不排除 mapper 实际已经成功完成, 只是上一轮观察窗口刚好停在“失败日志很多”的阶段.
- 如果后续统计显示注册率、相机连续性和点支持都正常, 那么“扩大上下文必然数值崩坏”这个判断就不能成立.

### 最小验证计划
- [ ] 回查 `v3_widecontext` 的真实 mapper 日志与退出状态
- [ ] 读取 `sparse/0` 的注册情况、相机内参和点云规模
- [ ] 用同口径脚本对 `view 7` 与相邻 pair 做连续性对照
- [ ] 再决定把它记为“成功但无改善”, 还是“数值不稳且结果不可采信”

### 当前状态
- 目前在“扩大局部上下文窗口结果核查”子任务.
- 下一步先追日志和模型摘要, 不直接沿用上一轮的口头判断.

## [2026-03-27 18:55:48 UTC] [Session ID: ebb3c562-1702-4ea2-8e98-ed18d0a9bada] [记录类型]: `v3_widecontext` 结果核查完成并回写正式结论

### 进度更新
- [x] 读取 `v3_widecontext sparse/0` 的注册情况、相机内参和点云规模
- [x] 用同口径脚本完成 `view 5 / 6 / 7 / 8` 与相邻 pair 的连续性对照
- [x] 更新正式报告与 `comparison_summary.json`
- [ ] 回查真实 mapper 退出日志
  - 当前目录里没有独立落盘的 mapper 日志.
  - 因此“运行期到底失败了多少轮 BA”这件事, 这轮无法再做更硬回放.

### 已验证结论
- `v3_widecontext` 不是“没产物”:
  - `images.bin` 中有 `48` 张图
  - `points3D.bin` 复查后是 `4710` 个稀疏点
- 这里要明确回滚一个旧假设:
  - 先前把 `read_points3D_binary()` 的 tuple 长度误读成了“点数 = 3”
  - 这个判断不成立
- 当前真正成立的结论是:
  - `widecontext` 的焦距从 `592.17` 漂到 `488.91`
  - `view 7 step_mean` 从全局的 `1.2833` 恶化到 `2.3200`
  - `7-8 delta_dist_mean` 从 `0.7345` 恶化到 `1.5666`
- 因此:
  - 这轮更大窗口实验没有支持“只要放大上下文就能把 `view 7` 修顺”这个假设
  - 当前更稳的方向仍然是筛帧对照或更保守的 mapper 参数

### 当前状态
- 当前“扩大局部上下文窗口结果核查”子任务已完成.
- 下一步如果继续 `my5`, 优先级调整为:
  - 先做带 frame-gap 口径保护的筛帧对照
  - 再考虑更保守的 mapper 参数
  - 暂不继续改窗口大小

## [2026-03-27 18:58:52 UTC] [Session ID: ebb3c562-1702-4ea2-8e98-ed18d0a9bada] [记录类型]: 为后续筛帧实验补 `frame_gap` 感知分析口径

### 目标
- 让 `scripts/analyze_colmap_pose_continuity.py` 在删帧后也能稳定给出不被 frame gap 污染的连续性统计.

### 最小执行计划
- [ ] 给 view / pair 明细补 `frame_gap`
- [ ] 为 summary 增加 `all` / `contiguous` 两种 transition 口径
- [ ] 增加最小单测覆盖 gap 场景
- [ ] 跑单测和最小脚本验证

### 当前状态
- 目前进入“连续性分析脚本 frame-gap 感知改造”子任务.
- 下一步直接修改现有脚本, 不再另外造新分析脚本.

## [2026-03-27 18:58:52 UTC] [Session ID: ebb3c562-1702-4ea2-8e98-ed18d0a9bada] [记录类型]: `frame_gap` 感知分析脚本改造完成

### 进度更新
- [x] 给 view / pair 明细补 `frame_gap`
- [x] 为 summary 增加 `all` / `contiguous` 两种 transition 口径
- [x] 增加最小单测覆盖 gap 场景
- [x] 跑单测和最小脚本验证

### 已验证结论
- `scripts/analyze_colmap_pose_continuity.py` 已支持:
  - `--transition-summary-mode all`
  - `--transition-summary-mode contiguous`
- 新增字段已在官方资产中落盘:
  - `frame_gap`
  - `is_contiguous_transition`
  - `transition_count`
  - `contiguous_transition_count`
  - `delta_transition_count`
  - `contiguous_delta_transition_count`
- 验证命令:
  - `python3 -m py_compile scripts/analyze_colmap_pose_continuity.py tests/test_analyze_colmap_pose_continuity.py`
  - `pixi run python -m unittest tests.test_analyze_colmap_pose_continuity tests.test_convert`
  - 真实冒烟:
    - `python3 scripts/analyze_colmap_pose_continuity.py ... --transition-summary-mode contiguous`

### 当前状态
- 当前“连续性分析脚本 frame-gap 感知改造”子任务已完成.
- 现在已经具备继续做 `view 7 frame 3 ~ 6` 筛帧对照的技术前提.

## [2026-03-27 19:08:08 UTC] [Session ID: ebb3c562-1702-4ea2-8e98-ed18d0a9bada] [记录类型]: 启动 `view 7 frame 3~6` 筛帧对照前先处理 `colmap` 可执行缺失

### 新现象
- 计划中的 `v4` 筛帧对照, 本来准备直接复用 `v3_widecontext` 口径:
  - `view 5 / 6 / 7 / 8`
  - `frame 1 ~ 12`
  - 只删 `view 7 frame 3 ~ 6`
- 但当前环境复查结果是:
  - `command -v colmap` 为空
  - `/workspace/colmap-cuda-install-3.12.6/bin/colmap` 不存在
  - `pixi` 环境里也找不到 `colmap`

### 当前主假设
- 这不是实验命令拼错.
- 更像是当前容器 / 机器环境和前几轮真实跑局部 COLMAP 的环境已经不同步.

### 最小执行计划
- [ ] 继续确认系统里是否存在别的 `colmap` 安装路径
- [ ] 如果确实不存在, 再决定是临时安装 CPU 版, 还是把本轮收口到“已准备好 v4 子集但无法启动重建”
- [ ] 无论哪种结果, 都把 blocker 和证据写回主线文件

### 当前状态
- 目前进入“筛帧实验前的执行环境核查”子任务.
- 下一步先确认有没有可复用 `colmap` 二进制, 再决定是否真正开跑 `v4`.
## [2026-03-27 19:20:12 UTC] [Session ID: 64a33e57-aa0e-4613-b103-207945436fa5] [记录类型]: `v4` 筛帧对照整理转入支线 `__my5_v4_filter_view7_f3to6`, 原因是主线 `task_plan.md` 已达 `999` 行, 后续状态与报告更新统一写入对应后缀六文件.
